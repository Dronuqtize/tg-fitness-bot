services:
  - type: web
    name: tg-fitness-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.bot
    envVars:
      - key: TZ
        value: Europe/Moscow
