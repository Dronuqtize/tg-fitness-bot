services:
  - type: worker
    name: tg-fitness-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.bot
    envVars:
      - key: TZ
        value: Europe/Moscow
      - key: PYTHON_VERSION
        value: 3.11.9
